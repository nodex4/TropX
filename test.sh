#!/bin/bash

:



## Turn this intoa one line if statem,ent checking for the text folding setting
#| fold -s -w "$fold_width"  | sed -e "s|^|\t|g"

# checkSettings "Text Folding"; textfolding=$value; if [[ "$textfolding" == "ON" ]]; then | fold -s -w "$fold_width"  | sed -e "s|^|\t|g"; fi
